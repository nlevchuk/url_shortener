import React from 'react';
import ReactDOM from 'react-dom';
import App from '../app';

describe('Test', () => {
  it('test 1', () => {
    /*assert.equal(1, 1);*/
    //expect(1).to.be.equal(1);
  });
});
